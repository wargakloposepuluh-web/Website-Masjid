version: '3.8'

services:
  simas-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: simas_persuratan
    restart: always
    ports:
      - "3000:3000"
    volumes:
      # Persistent storage untuk database SQLite lokal, upload file, dan sesi WhatsApp
      - ./prisma:/app/prisma
      - ./public/uploads:/app/public/uploads
      - ./storage:/app/storage
    environment:
      - NODE_ENV=production
